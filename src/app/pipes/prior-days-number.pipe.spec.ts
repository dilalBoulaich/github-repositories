import { PriorDaysNumberPipe } from './prior-days-number.pipe';

describe('PriorDaysNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorDaysNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
